#!/bin/bash
#PBS -N bulk_send_job
#PBS -l nodes=2:ppn=1
#PBS -l walltime=00:05:00
#PBS -o output.txt
#PBS -e error.txt

# Change to the directory where the job was submitted
cd $PBS_O_WORKDIR

echo "Running bulk send via loop"
mpiexec -n 2 ./bulk_send loop

echo ""
echo "Running bulk send via single array"
mpiexec -n 2 ./bulk_send bulk
